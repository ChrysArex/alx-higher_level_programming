-- script that creates a table in the current database in your MySQL server
-- database name will be passed as an argument of the mysql command
CREATE TABLE IF NOT EXISTS first_table (
  id INT,
  name VARCHAR(256)
);
